#ifndef ENTITY_H
#define ENTITY_H

#include "engine/component.h"

// creates a new entity in the engine and returns an (integer) handle to it
unsigned int new_entity();

// 
Component* get_component(int entity, enum type component_type);

// Iterator* get_mask_iterator(enum type component_type[]);


#endif
